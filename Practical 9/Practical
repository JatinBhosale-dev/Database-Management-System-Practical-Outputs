# Practical 9: Implement SQL Queries for Inner and Outer Join

**Question:** Implement SQL queries for Inner and Outer Join.

## 1. Concept Overview

Joins are used to combine rows from two or more tables based on a related column between them. 
*   **INNER JOIN:** Returns only the records that have matching values in both tables.
*   **LEFT (OUTER) JOIN:** Returns all records from the left table, and the matched records from the right table. The result is NULL from the right side if there is no match.
*   **RIGHT (OUTER) JOIN:** Returns all records from the right table, and the matched records from the left table. The result is NULL from the left side if there is no match.
*   **FULL OUTER JOIN:** Returns all records when there is a match in either the left or right table. 

*Viva Note:* MySQL does not natively support the `FULL OUTER JOIN` keyword. To achieve this in MySQL, we combine a `LEFT JOIN` and a `RIGHT JOIN` using the `UNION` operator.

---

## 2. Terminal & SQL Execution Steps

**Step 1: Accessing MySQL in Ubuntu Terminal**
Open your terminal (`Ctrl + Alt + T`) and log into the MySQL monitor.

```bash
mysql -u root -p
